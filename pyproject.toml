[tool.pytest.ini_options]
addopts = '--junitxml=reports/jreport.xml'
log_cli = true
log_cli_level = 5
log_format = '%(asctime)s | %(levelname)s | %(name)s | %(message)s'
log_date_format = '%Y-%m-%d %H:%M:%S'
